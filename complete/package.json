
{
  "name": "actions-builder-snow-pal-complete",
  "version": "0.0.0",
  "private": true,
  "description": "An Action that demonstrates an Interactive Canvas game.",
  "license": "Apache-2.0",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "lint": "eslint \"**/*.js\"",
    "lint:fix": "eslint --fix \"**/*.js\"",
    "test": "npm run lint",
    "serve": "firebase serve",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "eslint": "^5.2.0",
    "eslint-config-google": "^0.9.1"
  }
}
